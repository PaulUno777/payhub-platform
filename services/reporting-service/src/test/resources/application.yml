spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
management:
  tracing:
    sampling:
      probability: 1.0
    export:
      otlp:
        enabled: false
  endpoints:
    web:
      exposure:
        include: health,info
payhub:
  kafka:
    enabled: false
