spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
management:
  endpoints:
    web:
      exposure:
        include: health,info
payhub:
  kafka:
    enabled: false
