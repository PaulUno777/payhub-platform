spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
payhub:
  clients:
    payment-orchestrator: http://localhost:8200
