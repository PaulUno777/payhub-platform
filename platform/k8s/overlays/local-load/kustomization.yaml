apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
images:
  - name: ghcr.io/pauluno777/payhub-payment-orchestrator
    newName: pauluno/payhub-payment-orchestrator
    newTag: local
# IfNotPresent (not Never): multi-arch OCI indexes from GHCR retags fail with Never
# even after kind load. Load images first, then apply.
