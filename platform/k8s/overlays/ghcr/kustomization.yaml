apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
images:
  - name: ghcr.io/pauluno777/payhub-payment-orchestrator
    newTag: "0.2.0"
# Keep IfNotPresent (base): kind load + laptop registry DNS flakiness.
# True always-pull is not required for the DS-019 exit lab.
