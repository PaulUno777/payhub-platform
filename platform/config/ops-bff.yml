spring:
  application:
    name: ops-bff

payhub:
  security:
    issuer-uri: ${PAYHUB_OIDC_ISSUER:http://localhost:8090}
  clients:
    merchant-service: ${PAYHUB_CLIENTS_MERCHANT_SERVICE:http://localhost:8300}
    payment-orchestrator: ${PAYHUB_CLIENTS_PAYMENT_ORCHESTRATOR:http://localhost:8310}
    reconciliation-service: ${PAYHUB_CLIENTS_RECONCILIATION_SERVICE:http://localhost:8400}
