spring:
  datasource:
    url: jdbc:postgresql://postgres-reporting:5432/reporting
    username: ${DB_USERNAME:payhub}
    password: ${DB_PASSWORD:payhub}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
