spring:
  datasource:
    url: jdbc:postgresql://postgres-reconciliation:5432/reconciliation
    username: ${DB_USERNAME:payhub}
    password: ${DB_PASSWORD:payhub}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
